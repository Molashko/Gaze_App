[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    smoke: quick sanity tests
    calibration: calibration model tests
    gaze: gaze feature extraction tests
    head_pose: head pose estimation tests
    tracker: tracker orchestration tests
